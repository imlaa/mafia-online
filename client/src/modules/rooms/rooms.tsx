import React from 'react';

import { SectionTitle } from '@/components';

export const Rooms:React.FC = () => {
	return (
		<div>
			<SectionTitle title={'Rooms'} createButton={true} />
		</div>
	);
};
